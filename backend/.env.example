# Copy this file to .env and adjust values as needed.
# The app will also run fine with NO .env file at all — it falls back to sane defaults.

PORT=5000
JWT_SECRET=replace_this_with_a_long_random_secret_key
JWT_EXPIRES_IN=7d
